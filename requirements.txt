# JAX for high-performance training
jax>=0.4.20
jaxlib>=0.4.20
flax>=0.7.5
optax>=0.1.7
chex>=0.1.8

# Core dependencies
numpy>=1.21.0
matplotlib>=3.5.0
tqdm>=4.65.0
gym>=0.26.0
scipy>=1.9.0
pandas>=1.5.0
seaborn>=0.12.0

# Optional: PyTorch for comparison (can be removed if only using JAX)
# torch>=2.0.0
